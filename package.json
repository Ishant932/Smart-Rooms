{
  "name": "smartroooms",
  "version": "1.0.0",
  "description": "SmartRoooms - Student room rental platform",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "predev": "node scripts/free-ports.js",
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "start:local": "npm run build --prefix frontend && node backend/server.js",
    "open": "node scripts/open-browser.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
